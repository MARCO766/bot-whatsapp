# Solo si el CRM React está en OTRO dominio que el backend Express:
# VITE_API_BASE_URL=https://tu-app.up.railway.app

# Dev: proxy de Vite (opcional, default localhost:3000)
# VITE_DEV_API=http://localhost:3000
