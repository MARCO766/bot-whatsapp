# MacBot — deploy con Dockerfile (sin Nixpacks)
# Railway usa esta config; el build ocurre 100% en Dockerfile.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/flujos/status"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
