# MacBot — Railway: SOLO Dockerfile
# NO usar NIXPACKS. El build ocurre únicamente en ./Dockerfile

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/flujos/status"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
